export interface useFormModel {
    cardName: string,
    cardNumber: string,
    cardType: string,
    cardExpiration: string,
    cardSecurityCode: string,
    cardPostalCode: string,
    focus: string,
  
}